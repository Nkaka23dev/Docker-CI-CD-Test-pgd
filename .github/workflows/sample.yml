name: Shell commands

on: [push]

jobs: 
  run-shell-commads:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Welcome to the workflows"
      - name: Multiple script
        run: |
          node -v
          npm -v